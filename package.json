{
  "name": "pangea-countries",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run server.ts",
    "deploy": "wrangler pages deploy"
  },
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "wrangler": "^4.43.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "pako": "^2.1.0"
  }
}
